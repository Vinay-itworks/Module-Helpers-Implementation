<p style="color: green"><%= notice %></p>

<% content_for :title, "Bookings" %>

<h1>Bookings</h1>

<div id="bookings">
  <% @bookings.each do |booking| %>
    <%= render booking %>
    <p>
      <%= link_to "Show this booking", user_booking_path(id: booking.id) %>
    </p>
  <% end %>
</div>
<%= home_link %> |
<%= users_link %> |
<%= link_to "New booking", new_user_booking_path %>
