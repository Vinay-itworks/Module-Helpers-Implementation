<p style="color: green"><%= notice %></p>

<%= render @booking %>

<div>
  <%= home_link %> |
  <%= users_link %> |
  <%= link_to "Edit this booking", edit_user_booking_path(@booking) %> |
  <%= user_bookings_link %>

  <%= button_to "Destroy this booking", user_booking_path(@booking), method: :delete %>
</div>
